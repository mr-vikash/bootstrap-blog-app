<h1 class="text-center" >
    <%= @user.username %>
    <%= gravatar_for(@user, size: 200)  %>
    <h3 class="text-center mt-4">Articles</h3>
    <h3 class="text-center mt-4"><%= pluralize(@user.articles.count, "article") %></h3>
    <%= render "articles/article" %>
</h1>
